const { model } = require('mongoose');
const { User, Thought } = require('../models');

module.exports = {
    // Get all thoughts

    // Get single thought

    // Create a thought

    // Update a thought

    // Delete a thought

    // Create reaction

    // Delete reaction
};